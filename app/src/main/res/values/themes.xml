<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.DetectAI" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/text_color_primary</item>
    </style>

    <style name="Theme.DetectAI" parent="Base.Theme.DetectAI" />

    <!-- ✅ NEW: Splash Screen Theme -->
    <style name="Theme.DetectAI.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_custom</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.DetectAI</item>
    </style>
</resources>
